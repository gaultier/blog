[package]
name = "blog"
version = "0.1.0"
edition = "2024"

[dependencies]
# Live-reload HTTP server.
httparse = "1.10.1"
# Parse .md files.
markdown = "1.0.0"
# Watch files.
notify = "8.2.0"
# RSS feed.
uuid = {version="1.21.0", features=["v5"]}
